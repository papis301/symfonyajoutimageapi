<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Galerie d'images</title>
    <style>
        .gallery {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        .gallery img {
            width: 200px;
            height: auto;
            object-fit: cover;
            border: 1px solid #ccc;
            padding: 5px;
        }
    </style>
</head>
<body>
    <h1>Galerie d'images</h1>
    <div class="gallery">
        {% for image in images %}
            <img src="{{ asset('uploads/images/' ~ image.filename) }}" alt="Image {{ image.id }}">
        {% else %}
            <p>Aucune image enregistrée.</p>
        {% endfor %}
    </div>
</body>
</html>
